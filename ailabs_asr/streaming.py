def streaming(key: str):
  """
  ASR streaming to transcribe the voice to text
  
  Args:
      key (str): the key applied in dev-console
  Returns:
      None
  """
  
  print(f'Streaming with key: {key}')